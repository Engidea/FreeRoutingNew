import main.*;
import gui.*;
import iteractive.*;


public void tree_stats()
   {
   try
    {
    stat.log.userPrintln("tree_stats\n");

	aa=board_frame.board_panel.itera_board.get_routing_board().search_tree_manager.get_default_tree();
	print(aa.statistics());
    }
  catch ( Exception exc )
    {
    stat.log.exceptionShow("tree_stats()",exc);
    }
   }
